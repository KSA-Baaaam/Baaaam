---
paths:
  - "src/**/*.css"
  - "src/**/*.scss"
---

# Style contract

- Tailwind 4 and `@theme` are supported by the template's Tailwind Vite plugin.
- CSS Modules and local CSS `url()` references are supported.
- Keep global styles in the existing source style entry unless the feature benefits from a scoped
  module.
